apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: nlp-root-redirect
spec:
  redirectRegex:
    regex: ^https?://abvc-surveillance\.org/?$
    replacement: https://abvc-surveillance.org/nlp
    permanent: false
